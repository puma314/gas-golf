# SP1 Gas Golfing Example

After installing SP1 (https://docs.succinct.xyz/getting-started/install.html), run the following commands:

```
cd script;
RUST_LOG=info cargo run --release -- --execute --n 100000
```
